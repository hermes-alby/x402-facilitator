# HTTP server port
PORT=3000

# NOTE: NWC_URL is no longer configured here.
# This facilitator is multi-tenant — each merchant passes their own
# Nostr Wallet Connect URL (nostr+walletconnect://...) in the payment
# requirements (extra.nwcUrl) when requesting an invoice.
# The facilitator creates invoices and looks up settlements against
# each merchant's wallet independently.
